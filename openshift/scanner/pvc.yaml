apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sbom-scanner-reports
  labels:
    app: sbom-scanner
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
